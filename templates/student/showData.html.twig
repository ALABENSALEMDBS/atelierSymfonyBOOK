<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>Show Data</title>
</head>
<body>
    <div class="container mt-5">
        <h2>Affichage liste etudiants</h2>

        <table class="table">
            <thead>
                <tr>
                    {% for column in columns %}
                    <th scope="col">{{column | capitalize}}</th>
                    {% endfor %}
                </tr>
            </thead>
            <tbody>
                {% for record in data %}
                <tr>
                    <th scope="row">{{record.id}}</th>
                    <td>{{record.name }}</td>
                    <td>{{record.prenom}}</td>
                    <td>{{ record.dateNaissance ? record.dateNaissance|date('Y-m-d') : '' }}</td>
                    <td>{{record.class |upper}}</td>
                    <td>{{record.moyenne}}</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>

    

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-4Ww1fY2Dmj+IohMNGX56InGrsl6iE92D7+FFHIe4v9CCwAr5vvH4hf5nU8l06/5g" crossorigin="anonymous"></script>
</body>
</html>
